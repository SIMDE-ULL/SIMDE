import test from 'ava';
import { DependencyChecker } from '../../../../core/VLIW/DependencyChecker';

//TODO: replace this dummy test template

test('Class can be constructed', t => {
    t.notThrows(() => {
        new DependencyChecker();
    });
})