export class ContentIntegration {

    private FPRContent: { [k: number]: number } = {}
    private GPRContent: { [k: number]: number } = {}
    private MEMContent: { [k: number]: number } = {}

    constructor(private input: string) {

    }

    
}